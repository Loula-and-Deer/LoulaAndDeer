{% for p in item.properties %}
<p style="margin: 0">
    {% unless p.last == blank %}
        {{ p.first }}:
        {% if p.last contains '/uploads/' %}
        <a class="lightbox" href="{{ p.last }}">{{ p.last | split: '/' | last }}</a>
        {% else %}
        {{ p.last }}
        {% endif %}
        <br />
    {% endunless %}
</p>
{% endfor %}